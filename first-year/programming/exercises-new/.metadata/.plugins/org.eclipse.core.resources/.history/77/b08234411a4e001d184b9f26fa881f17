#include <iostream>
using namespace std;

bool iSInArray(int array[], int length, int n) {
	for (int i = 0; i < length; i++) {
		if (array[i] == n) isInArray = true;;
	}

	return pal;
}

int main() {
	int l;
	cout << "Insert the length: ";
	cin >> l;

	int a[l];
	for (int i = 0; i < l; i++) {
		int x;
		cout << "Insert a number: ";
		cin >> x;
		a[i] = x;
	}

	if (isPalindrome(a, l)) cout << "The array is palindrome";
	else cout << "The array is not palindrome";

	return 0;
}
