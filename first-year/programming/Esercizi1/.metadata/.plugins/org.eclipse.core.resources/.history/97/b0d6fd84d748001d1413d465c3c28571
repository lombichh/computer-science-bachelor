#include <cmath>
#include <iostream>
using namespace std;

int reverseInt(int num) {
	int reversedNum = 0;

	while (num > 0) {
		reverseNum += num % 10;
	}

	return decimal;
}

int main() {
	int n;
	cin >> n;

	cout << reverseInt(n);

	return 0;
}
